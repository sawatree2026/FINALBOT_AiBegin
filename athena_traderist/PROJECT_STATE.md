# 🏛️ PROJECT_STATE.md — Single Source of Truth (SSOT)
> **บันทึกสถานะและความจำกลางของระบบ Athena Trader ข้ามทุกแพลตฟอร์ม (IDE / CLI / Web)**  
> *อัปเดตล่าสุด: 2026-09-24*

---

## 👑 1. ตัวตนและกฎเหล็กประจำตัว (Identity & Strict Rules)
- **ตัวตน:** เอเธน่า (Athena) เลขาธิการส่วนตัวของบอส ฉลาด กระชับ ตรงเป้าหมาย
- **คำลงท้าย:** ใช้ "ค่ะ" เสมอ สื่อสารภาษาไทยเท่านั้น
- **กฎประหยัด Token สูงสุด:**
  1. ตอบสั้น ตรงเป้าหมาย ห้ามทักทาย ห้ามขอโทษ ห้ามทวนคำถาม และห้ามลงท้ายสรุป
  2. อธิบายไม่เกิน 2-3 บรรทัด ส่งผลลัพธ์หรือคำสั่งปฏิบัติการทันที
  3. ห้ามสแกนไฟล์นอกคำสั่ง ห้ามอ่านไฟล์ซ้ำซ้อน ยึด `PROJECT_STATE.md` เป็นความจำหลัก
  4. งานต้อง verify และทดสอบจริงบนพอร์ต DEMO เสมอ

---

## 📊 2. สถาปัตยกรรมระบบบอทอิสระ (Athena Trader Architecture)
- **โฟลเดอร์ระบบ:** `e:\FINALBOT_AiBegin\Athena_trader\` (แยกขาดอิสระ 100% จาก FINALBOT เดิม)
- **บัญชีโบรกเกอร์:** IQ Option บัญชี **DEMO / PRACTICE** เท่านั้น
- **การส่งข้อมูลและคำสั่ง (Decoupled IPC):**
  - บอททำหน้าที่เป็นมือเท้า: ดึงแท่งเทียนสดจากโบรกเกอร์ และส่งคำสั่งซื้อขายเข้าโบรกเกอร์
  - เอเธน่าทำหน้าที่เป็นสมอง: วิเคราะห์แท่งเทียนตามตำรา Nemesis และสั่งยิงออเดอร์
  - กล่องข้อความกลาง: `Athena_trader/signals/market_feed.json` และ `order_command.json`

---

## 📈 3. คลังกลยุทธ์การเทรด (Nemesis E-Books V1 & V2)
- **เกณฑ์คัดเลือกสินทรัพย์:** Payout ต้อง $\ge 85\%$ เท่านั้น
- **คู่เทรดหลักปัจจุบัน (ตลาดจริง Options):**
  - `EURUSD-op` (Payout 90%)
  - `GBPUSD-op` (Payout 90%)
  - `GBPJPY-op` (Payout 87%)
  - `EURJPY-op` (Payout 87%)
- **กลยุทธ์หลัก (Believe Strategy):**
  - **Timeframe:** แท่งเทียน `S30` (30 วินาที) สำหรับจังหวะเข้า (Entry)
  - **ระยะถือครอง (Expiry):** `5 นาที`
  - **ตัวกรองระดับ M1:** ใช้ตรวจจับแนวรับต้าน EUF (`RG DOWN` = แนวต้าน, `GR UP` = แนวรับ)
  - **เงื่อนไข CALL:** BB%B แตะแนวล่าง (<=0.10) + STO (13-10-3) Hook Up ข้าม 50 + MA 3 ตัด MA 6 ขึ้น
  - **เงื่อนไข PUT:** BB%B แตะแนวบน (>=0.90) + STO (13-10-3) Hook Down ข้าม 50 + MA 3 ตัด MA 6 ลง
  - **กฎเหล็กความปลอดภัย:** ห้ามมีแท่งเทียนสีเทา/โดจิ, STO ห้ามพันกัน (Tangled), ห้ามมีแนวรับต้าน/กริดขวางหน้า

---

## 🔄 4. โปรโตคอลการทำงานข้ามแพลตฟอร์ม (Cross-Platform Memory Protocol)
ทุกอินเทอร์เฟซ (CLI, IDE, Web) ต้องปฏิบัติตามมาตรฐานเดียวกัน:
1. **เมื่อเริ่มเซสชัน:** อ่านสรุปสถานะจาก `PROJECT_STATE.md` นี้ทันทีโดยไม่ต้องสแกนหาไฟล์อื่น
2. **เมื่อมีคำสั่งซื้อขายหรือผลลัพธ์ใหม่:** อัปเดตบันทึกสถานะลงในไฟล์นี้ทันที
3. **การสั่งการบอทจาก Terminal/CLI:**
   - ตรวจสอบตลาด: `python -u e:\FINALBOT_AiBegin\Athena_trader\fetch_feed.py --symbol EURUSD-op --tf S30`
   - ยิงออเดอร์ทดสอบ: `python -u e:\FINALBOT_AiBegin\Athena_trader\send_order.py --symbol EURUSD-op --action CALL --amount 35 --duration 5`
   - รันลูปเทรดอัตโนมัติ: `python -u e:\FINALBOT_AiBegin\Athena_trader\run_athena.py`

---

## 💰 5. บันทึกสถานะพอร์ตล่าสุด (Live Account State)
- **โหมดบัญชี:** DEMO / PRACTICE
- **ยอดเงินคงเหลือล่าสุด:** ~$10,075.30 - $10,110.30
- **ผลการทดสอบยิงออเดอร์สด:** Order ID `14290330953` (EURUSD-OTC, CALL $100) -> ผลลัพธ์: **WIN (ITM)** กำไร +$82.00
